package com.brewbite.model.user;

/**
 * Represents the three distinct roles in the Brew & Bite system.
 * Each role grants access to a different set of features.
 */
public enum UserRole {
    CUSTOMER,
    BARISTA,
    MANAGER
}
